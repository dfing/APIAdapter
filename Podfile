# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'APIAdapter' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for APIAdapter
  pod 'Alamofire'
  pod 'Moya', '~> 11.0'
  pod 'SwiftyJSON'
  pod 'PromiseKit', '~> 6.0'

end
